<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.interviewagent.mapper.InterviewEvaluationMapper">

    <insert id="insert" parameterType="com.example.interviewagent.entity.InterviewEvaluation"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO interview_evaluation (
            session_id, question_record_id, answer_id, score, max_score,
            level, matched_points, missing_points, suggestion, evaluator_type
        )
        VALUES (
            #{sessionId}, #{questionRecordId}, #{answerId}, #{score}, #{maxScore},
            #{level}, #{matchedPoints}, #{missingPoints}, #{suggestion}, #{evaluatorType}
        )
    </insert>

    <select id="selectById" resultType="com.example.interviewagent.entity.InterviewEvaluation">
        SELECT *
        FROM interview_evaluation
        WHERE id = #{id}
    </select>

    <select id="selectByQuestionRecordId" resultType="com.example.interviewagent.entity.InterviewEvaluation">
        SELECT *
        FROM interview_evaluation
        WHERE question_record_id = #{questionRecordId}
        LIMIT 1
    </select>

    <select id="selectBySessionId" resultType="com.example.interviewagent.entity.InterviewEvaluation">
        SELECT *
        FROM interview_evaluation
        WHERE session_id = #{sessionId}
        ORDER BY id ASC
    </select>

</mapper>
