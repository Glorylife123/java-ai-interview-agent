<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.interviewagent.mapper.UserAnswerRecordMapper">

    <insert id="insert" parameterType="com.example.interviewagent.entity.UserAnswerRecord"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user_answer_record (
            user_id, question_id, user_answer, is_correct, score,
            time_cost_seconds, answer_source
        )
        VALUES (
            #{userId}, #{questionId}, #{userAnswer}, #{isCorrect}, #{score},
            #{timeCostSeconds}, #{answerSource}
        )
    </insert>

    <select id="selectByUserId" resultType="com.example.interviewagent.entity.UserAnswerRecord">
        SELECT *
        FROM user_answer_record
        WHERE user_id = #{userId}
        ORDER BY created_at DESC
    </select>

</mapper>
