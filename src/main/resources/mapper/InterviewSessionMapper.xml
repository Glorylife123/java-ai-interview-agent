<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.interviewagent.mapper.InterviewSessionMapper">

    <insert id="insert" parameterType="com.example.interviewagent.entity.InterviewSession"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO interview_session (
            user_id, title, position, difficulty, status,
            total_question_count, current_question_index, started_at, ended_at
        )
        VALUES (
            #{userId}, #{title}, #{position}, #{difficulty}, #{status},
            #{totalQuestionCount}, #{currentQuestionIndex}, #{startedAt}, #{endedAt}
        )
    </insert>

    <select id="selectById" resultType="com.example.interviewagent.entity.InterviewSession">
        SELECT *
        FROM interview_session
        WHERE id = #{id}
    </select>

    <!-- 动态更新：仅更新非空字段，避免把未变更字段覆盖为 null。 -->
    <update id="update" parameterType="com.example.interviewagent.entity.InterviewSession">
        UPDATE interview_session
        <set>
            <if test="title != null">title = #{title},</if>
            <if test="position != null">position = #{position},</if>
            <if test="difficulty != null">difficulty = #{difficulty},</if>
            <if test="status != null">status = #{status},</if>
            <if test="totalQuestionCount != null">total_question_count = #{totalQuestionCount},</if>
            <if test="currentQuestionIndex != null">current_question_index = #{currentQuestionIndex},</if>
            <if test="startedAt != null">started_at = #{startedAt},</if>
            <if test="endedAt != null">ended_at = #{endedAt},</if>
        </set>
        WHERE id = #{id}
    </update>

    <select id="selectByUserId" resultType="com.example.interviewagent.entity.InterviewSession">
        SELECT *
        FROM interview_session
        WHERE user_id = #{userId}
        ORDER BY created_at DESC
    </select>

    <select id="countByUserId" resultType="int">
        SELECT COUNT(*)
        FROM interview_session
        WHERE user_id = #{userId}
    </select>

</mapper>
