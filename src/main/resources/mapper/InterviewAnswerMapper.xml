<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.interviewagent.mapper.InterviewAnswerMapper">

    <insert id="insert" parameterType="com.example.interviewagent.entity.InterviewAnswer"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO interview_answer (
            session_id, question_record_id, user_id, answer_content, duration_seconds
        )
        VALUES (
            #{sessionId}, #{questionRecordId}, #{userId}, #{answerContent}, #{durationSeconds}
        )
    </insert>

    <select id="selectById" resultType="com.example.interviewagent.entity.InterviewAnswer">
        SELECT *
        FROM interview_answer
        WHERE id = #{id}
    </select>

    <select id="selectByQuestionRecordId" resultType="com.example.interviewagent.entity.InterviewAnswer">
        SELECT *
        FROM interview_answer
        WHERE question_record_id = #{questionRecordId}
        LIMIT 1
    </select>

    <select id="selectBySessionId" resultType="com.example.interviewagent.entity.InterviewAnswer">
        SELECT *
        FROM interview_answer
        WHERE session_id = #{sessionId}
        ORDER BY id ASC
    </select>

</mapper>
