<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.interviewagent.mapper.QuestionTagMapper">

    <insert id="insertIgnore">
        INSERT IGNORE INTO question_tag (question_id, tag_id)
        VALUES (#{questionId}, #{tagId})
    </insert>

    <select id="selectTagsByQuestionId" resultType="com.example.interviewagent.entity.Tag">
        SELECT t.*
        FROM tag t
        INNER JOIN question_tag qt ON t.id = qt.tag_id
        WHERE qt.question_id = #{questionId}
          AND t.deleted = 0
        ORDER BY t.category, t.name
    </select>

    <!-- 分页题目统一补齐标签，避免逐题调用标签接口造成 N+1 查询。 -->
    <select id="selectRelationsByQuestionIds" resultType="com.example.interviewagent.service.dto.QuestionTagRelation">
        SELECT qt.question_id AS question_id,
               t.id AS tag_id,
               t.name AS tag_name,
               t.category AS tag_category
        FROM question_tag qt
        INNER JOIN tag t ON t.id = qt.tag_id
        WHERE qt.question_id IN
        <foreach collection="questionIds" item="questionId" open="(" separator="," close=")">
            #{questionId}
        </foreach>
          AND t.deleted = 0
        ORDER BY qt.question_id, t.category, t.name
    </select>

    <delete id="deleteByQuestionId">
        DELETE FROM question_tag
        WHERE question_id = #{questionId}
    </delete>

    <insert id="insertBatch">
        INSERT INTO question_tag (question_id, tag_id)
        VALUES
        <foreach collection="tagIds" item="tagId" separator=",">
            (#{questionId}, #{tagId})
        </foreach>
    </insert>

</mapper>
