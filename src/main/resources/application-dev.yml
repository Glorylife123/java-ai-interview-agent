spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/interview_agent?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    # 优先读取环境变量 DB_PASSWORD；本地默认值仅用于开发环境
    password: Hdcz3691

mybatis:
  configuration:
    map-underscore-to-camel-case: true

# 本地开发通常走 http，Secure Cookie 不会被浏览器回传，这里关闭以方便调试
app:
  jwt:
    cookie-secure: false
